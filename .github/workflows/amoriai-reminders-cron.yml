name: AmorIAI — Reminders Cron

on:
  schedule:
    - cron: "0 */6 * * *" # toutes les 6 heures
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call reminders endpoint
        run: |
          curl -sS "https://www.amoriai.app/api/cron/reminders?secret=${{ secrets.CRON_SECRET }}"
